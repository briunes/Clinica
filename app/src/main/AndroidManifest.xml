<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clinica">
        <activity
            android:name=".Contacts"
            android:label="@string/title_activity_contacts"
            android:theme="@style/Theme.Clinica.NoActionBar"></activity>
        <activity android:name=".CreateAppointment" />
        <activity
            android:name=".GetAppointments"
            android:label="@string/title_activity_get_appointments"
            android:theme="@style/Theme.Clinica.NoActionBar" />
        <activity
            android:name=".WelcomePage"
            android:label="@string/title_activity_welcome_page"
            android:theme="@style/Theme.Clinica.NoActionBar" />
        <activity android:name=".GetDocuments" />
        <activity android:name=".GetReceipts" />
        <activity android:name=".ChatMedic" />
        <activity android:name=".Documents" />
        <activity android:name=".GetDados" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>